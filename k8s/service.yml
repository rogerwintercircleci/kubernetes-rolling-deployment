apiVersion: v1
kind: Service
metadata:
  name: rolling-tutorial-app
  namespace: default
spec:
  selector:
    app: rolling-tutorial-app
  ports:
    - port: 80
      targetPort: 3000
  type: LoadBalancer
